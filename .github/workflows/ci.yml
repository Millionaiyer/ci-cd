- name: Install dependencies
  run: |
    npm install

- name: Install Playwright browsers
  run: |
    npx playwright install --with-deps

- name: Run lint
  run: |
    npm run lint

- name: Run build
  run: |
    npm run build

- name: Run tests
  run: |
    npm test
